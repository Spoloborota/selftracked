{
  "permissions": {
    "deny": [
      "Bash(sqlite3:*)",
      "Bash(git push:*)",
      "Bash(git push)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'selftracked prime --json 2>/dev/null || { selftracked load >/dev/null 2>&1 && selftracked prime --json; } || echo \"{\\\"error\\\":\\\"selftracked state unavailable\\\",\\\"hint\\\":\\\"install selftracked (go install ./cmd/selftracked) and run selftracked load; see .selftracked/dump.sql\\\"}\"'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 .claude/hooks/assertion_check.py"
          }
        ]
      }
    ]
  }
}
