# Project state

Generated by `selftracked state`; do not hand-edit (the pre-commit hook regenerates it). Numbers here are the database's, not a copy — see `selftracked list`/`show`/`log` for detail.

## Active epics

- **adoption-contract** — Make the materials that init installs sufficient for a fresh agent's first hour. The generated contract describes the verbs but never says how to invoke the tool, which tracker it is operating on, what language its content should be in, or that a whole class of writes is refused against a closed epic; the migration guide documents neither the md-table format it offers nor the remote a copy-on-write rehearsal copy inherits, and the reconcile recipe covers only one of the two directions divergence can take. Every item was hit in practice during the pilot rehearsals. The task-versus-story rule this goal originally listed was delivered by epic tracking-integrity (#55) and is not scope here.
  (stories: 6 done, 0 in progress, 0 ready, 0 blocked)

## Task queue

- open: 39
- in review: 1
- needs triage: 1

## Recent activity

- `2026-07-26T12:13:10Z` #80 set-status: AWAITING OWNER - this is the one obligation keeping epic adoption-contract from closing (every story terminal, all six criteria met 2026-07-26; epic close refuses naming this task). The gap, measured when filed: the drift gate refuses a symlinked FILE on either side of an installed/template pair, but a symlinked PARENT directory passes at exit 0 with three ok lines; reproduced in a clean repository - git status itself shows the parent case LOUDER (' D d/f.txt' plus an untracked entry) than the file case (a single 'T' typechange), so the gate merely lacks a second line of defence for a case git already surfaces. Three routes, each the owner's: (1) fix it - a path-component walk in the gate script; small, but work advancing an ACTIVE epic's goal is a story, and opening one is the owner's scope call; (2) WONT-DO on the measured grounds recorded here - the gate exists to catch SILENT drift and this case is not silent; (3) edit --detach, un-homing it as generic tool debt so the epic closes and the task lives on epicless. The coordinating agent's recommendation is (2), with (1) as a one-story follow-up if the owner wants the second detector anyway; (3) is the weakest - it keeps the debt while removing its home.
- `2026-07-26T12:12:08Z` epic:adoption-contract criteria: met 2: Verified 2026-07-26 by the coordinating agent against the installed PROMPT.md. The first-hour refusal classes are each named with their routes: not-found (both directions - the pre-init case advising init, and the subdirectory case naming the found root, which since story S4 the binary actually produces); the version gate and the migration-interrupted sentinel; dump divergence with both recoveries and the test selecting between them; control-chars text hygiene; and the terminal-epic class, named in its own section with the exact code {"code":"terminal"}, exit 1, the closed list of what stays open after a close (V-rows, corrections, artifact links, edit --detach, the provenance-scoped story dissolve carve-out), and the non-route stated outright (no epic reopen). Exercised live this session: the S4 refusals; the terminal list matches section 6.4 as revised at rev 3.43.
- `2026-07-26T12:11:56Z` epic:adoption-contract criteria: met 1: Verified 2026-07-26 by the coordinating agent against the installed PROMPT.md (byte-equal to its template per the check-copies gate, make gates exit 0). How to invoke: the three-case section (PATH, repo-local build, absolute path) with the one probe line, prime, and its three named refusal shapes. Which tracker: PROMPT.md's promise that a tracker not in the working directory is not-found naming the root it did find is now the binary's behaviour (story S4: live from internal/, list exits 1 naming .. with no init advice, resolution.txtar pins all five sites), and prime opens with the instance identity in JSON and digest alike. Task versus story: the 'Where new work goes' section states the rule, the scope-change ownership, and the drift-rule reconciliation (landed by tracking-integrity #55, byte-identical template and installed copy). No knowledge from outside the repository is needed for any of the three.
- `2026-07-26T12:11:07Z` epic:adoption-contract/S4 story: done: 91d87b1..649a5ea; gate: make gates exit 0 (2026-07-26); resolution.txtar exercises all five refusal sites plus the pre-init case, prime.txtar pins the instance field's first position, stability and gitignored salt; instance/prime unit tests green under -race; live from internal/: list exits 1 naming .. with no init advice; prime --json opens {"instance":"80f763391ff9",...}
- `2026-07-26T12:10:46Z` #77 set-status: Decided while the walk was written, as this task asked (story S4, commits 1e72904+649a5ea): the bound stays the filesystem root, which is also what the applied section 6.1 text specifies - any narrower bound (git boundary, mount, home) would now change a shipped refusal's behaviour and so travels the amendment flow. Grounds, recorded in AncestorTracker's comment: the walk only improves a refusal and operates on nothing; the root it names is factually where a tracker sits whoever owns it; and after the review round a candidate must hold db.sqlite or dump.sql, which removes the cheapest stranger-ancestor confusion (a bare directory named .selftracked). The residual case - a genuine unrelated tracker near the user's home - remains message-quality, as adjudicated when this task was filed, and now requires a real tracker to trigger.
- `2026-07-26T12:10:35Z` #76 set-status: Closed by story S4, commits 1e72904+649a5ea: all three mechanics met as decisions, recorded in internal/instance/instance.go's ensureSalt comment and pinned by tests. (1) Creation is exclusive and the WRITE is symlink-safe: temp file + hard link, since link(2) never follows a pre-existing name; the READ half deliberately follows a pre-existing file or symlink per ADR 0001's standing boundary - the review round's attempt to rate that a vulnerability was refuted against the ADR, which predicted the rediscovery. (2) Mode is 0600, enforced by explicit Chmod, matching the sidecar's posture. (3) A creation race resolves by the loser reading the winner's fully-written salt - the link lands only after close, so no partial read exists and the digest never changes between runs; TestEnsureSaltConcurrent races 16 goroutines under -race and finds one salt, no residue. Bonus from the round: instance.salt* is the gitignore pattern, so a killed process's temp residue stays invisible to git status.
- `2026-07-26T12:10:35Z` #75 set-status: Closed with story S4: commit 1e72904 lands the last two of the eight amendments (a-tracker-carries-a-name; resolution-names-the-root-it-found) and removes the paragraph headed 'Where this document runs ahead of the code' from docs/v0-spec.md in the same commit, as this task required and as the notice itself promised. The revision history records the removal (rev 3.44) naming exactly the two amendments, no count. The migration guide's notice was already gone at S7. Verified: grep 'runs ahead' docs/v0-spec.md matches only the historical revision-log sentence describing the removal.
- `2026-07-26T12:10:20Z` #47 set-status: Closed by story S4 of epic adoption-contract, commits 1e72904+649a5ea, under the accepted amendment resolution-names-the-root-it-found (revs 3.39/3.42). All five inventoried refusal sites - the shared pipeline, dump, verify (the site a pipeline-only fix would miss), gate skip-mark (reconciled deliberately, keeping its directory-shaped first clause), and load (its own missing-dump clause) - walk upward on the failure path only and, from a subdirectory of a tracked repository, name the found root relative to the working directory with no init advice; nothing-anywhere keeps the historical message byte-verbatim; an unreadable candidate ends the walk; the walk is physical and stat-only, and after the review round a candidate must actually hold db.sqlite or dump.sql, so a bare directory carrying the name cannot capture the refusal. Reproduced fixed 2026-07-26: from internal/, selftracked list = exit 1, code not-found, 'a tracker exists at .. - run selftracked from that directory'. Evidence: resolution.txtar exercises all five sites plus the pre-init case; make gates exit 0; FULL review round; #77's bound question decided in the walk's own comment; #88 parks the logical-path sharpening.
- `2026-07-26T12:10:08Z` #24 set-status: Closed by story S4 of epic adoption-contract, commits 1e72904+649a5ea. The ratified shape (amendment a-tracker-carries-a-name, the pair with resolution-names-the-root-it-found) puts the identity where the agent forms its model: prime's stable contract opens with instance - 12 hex of SHA-256(salt || physically-resolved path), salted per-tracker, gitignored, omitted on either failure mode, never downgraded - and the human digest's first token is the same value; write verbs stay unchanged by that ratified design, with the load/import class of #86 remaining the pair's stated limit. The installed contract's promise (PROMPT.md: a tracker not in the working directory is not-found naming the root it did find) is now true of the binary: verified live from internal/, selftracked list exits 1 naming '.. ' with no init advice. Evidence: make gates exit 0; prime --json opens {"instance":"80f763391ff9",... on this repository; resolution.txtar and prime.txtar pin both surfaces; FULL three-lens review round adjudicated.
- `2026-07-26T12:07:25Z` #88 park: Amendment-flow question: the spec mandates the physical basis, and no incident has hit this case; revisit when a reader is actually misled or the next resolution amendment opens
