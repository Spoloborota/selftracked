# Project state

Generated by `selftracked state`; do not hand-edit (the pre-commit hook regenerates it). Numbers here are the database's, not a copy — see `selftracked list`/`show`/`log` for detail.

## Active epics

- **adoption-contract** — Make the materials that init installs sufficient for a fresh agent's first hour. The generated contract describes the verbs but never says how to invoke the tool, which tracker it is operating on, what language its content should be in, or that a whole class of writes is refused against a closed epic; the migration guide documents neither the md-table format it offers nor the remote a copy-on-write rehearsal copy inherits, and the reconcile recipe covers only one of the two directions divergence can take. Every item was hit in practice during the pilot rehearsals. The task-versus-story rule this goal originally listed was delivered by epic tracking-integrity (#55) and is not scope here.
  (stories: 5 done, 0 in progress, 0 ready, 0 blocked)

## Task queue

- open: 44
- in review: 0
- needs triage: 0

## Recent activity

- `2026-07-26T11:19:39Z` #77 edit: note: →HANDOFF for story S4. Raised by the stan…
- `2026-07-26T11:19:39Z` #76 edit: note: →HANDOFF for story S4. Raised by the stan…
- `2026-07-26T11:19:39Z` #75 edit: note: →HALF DONE as of 2026-07-26, and the rema…
- `2026-07-26T11:19:19Z` #82 edit: note: Incident 2026-07-26. A critic running th…→Incident 2026-07-26, the FIRST of two on…
- `2026-07-26T11:19:19Z` #47 edit: note: The CLI resolves .selftracked/ strictly …→Run from a subdirectory, verbs recommend…
- `2026-07-26T11:18:59Z` #24 edit: note: A verb resolves .selftracked/ from the w…→Verbs never say which tracker they opera…
- `2026-07-26T11:18:25Z` epic:adoption-contract criteria: met 6: Met by stories S6 and S7, verified 2026-07-26 by the coordinating agent. One name: the criterion text is text on both doors with criterion an accepted alias on both, the verb taking them as one flag rather than as a flag plus a better error, and each refusal naming the other spelling. Exercised live: both flags write the same list, both with different values refuse usage, neither refuses naming both, and a corpus using either spelling imports while a third guess is redirected to both. The DDL is byte-equal before and after, sha 8bbb8daec5f8347b from binaries built at HEAD and at the change. The gate: make gates runs scripts/check-installed-copies.sh, proven by the coordinating agent to exit 1 on a one-byte divergence, on a missing copy, on an emptied pair table and on a symlink standing in for a copy, and 0 on identity - with its red fixture running in the same step.
- `2026-07-26T11:18:25Z` epic:adoption-contract criteria: met 5: Met by story S2, verified 2026-07-26 by the coordinating agent against a throwaway tracker. No import path ends green with nothing imported: a bare pipe table with no heading, an empty JSON object, and a corpus whose recognized sections are all empty each refuse with code format and exit 1, and the two reasons carry different messages because they call for different actions. A paths-only corpus still imports green with a non-zero path count, which is why the counts line gained its fifth kind. The rule sits at the verb rather than in a reader, since both readers produce the empty corpus. docs/migration-guide.md section 7.1 states what a repeat or delta import does to an epic already closed - the children-only shape lands under it while a re-declaration is refused epic-exists - and names the R16 advisory as where the operator learns of it, together with the repair each finding names.
- `2026-07-26T11:18:25Z` epic:adoption-contract criteria: met 4: Met by stories S2 and S3, verified 2026-07-26. The installed PROMPT.md carries all three. Language: tracker content is one language chosen once per repository, English the shipped default, stated with the reasons that are properties of this artifact and marked a default rather than a prohibition, including which of the three reasons argue for one language and which for English. Invocation: the binary, the alias, PATH, the two fallbacks stated as shapes never as literal paths, and prime as the one-line check that replaces probing. Divergence: both directions, the git test that selects between them, the rule that the good side is the one holding work that exists nowhere else, the two-writer case where neither branch is safe, and the bookkeeping commit without which the divergence is unresolved on the git side. No knowledge from outside the repository is needed for any of the three.
- `2026-07-26T11:18:25Z` epic:adoption-contract criteria: met 3: Met by stories S1 and S2, verified 2026-07-26. docs/migration-guide.md section 4.1 documents the md-table format where the reader meets it: the five lower-case exact section headings, the per-section column names checked against import_mdtable.go's knownColumns, that a repeated section is refused as a duplicate, that it carries no criteria section at all while it does carry worklog and future_increment, and that a corpus inserting nothing is refused. Sections 6 and 8 keep the two rehearsal shapes apart on the corrected mechanism - a local git clone points origin at the source path, a copy-on-write copy inherits the source's configured remote verbatim - and make severing that remote the first act on the copy, with its expectation stated as a command whose exit code the reader can check.
