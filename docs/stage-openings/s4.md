# S4 opening record

Stage: S4 — `load` + parser fuzzing (plan §4). Opened 2026-07-19, per
D-EP13. Spec revision at open: 3.13. Plan revision at open: 12.
Rows owned at open: 18. After the open: 17 at S4 — one moved (below).

S4's machinery: everything through S3 (schema, dispatcher, serializer,
`dump`), plus what it builds — the §8.5 whitelist loader, the security
boundary. One architectural consequence surfaced at this open rather than
mid-code: **INV-373 obliges `load` to run Stage-0 verify plus the DB-only
§7 rules (R6–R9, R12) before the rename** — rules whose own inventory rows
belong to S7. So S4 births the shared rules engine for exactly that
subset as an internal package; S7 later wraps the `verify` verb around the
same code and closes each rule's own rows with their full fixtures. S4's
close proves only what INV-373 names: a forged boundary or a trail-less
terminal state never survives to the rename.

**Resolved verification command.** Parser/loader fixtures:
`go test ./internal/load -run 'TestLoad/<fixture>'`; the fuzz target is
`go test ./internal/load -run '^$' -fuzz FuzzWhitelistParser -fuzztime 10s`;
verb-level flows run as testscript scenarios.

## Rows moved (placement defects)

| Row | To | Why |
|---|---|---|
| INV-277 | S5a | "The NEXT WRITE VERB, or `load`, converts the pending skip marker" — the write-verb half has no executor before S5a, and by S5a both halves exist (load included). A row cannot half-close at S4. |

## Per-row verdicts

| Row | Content | Placement | Verification (resolved) |
|---|---|---|---|
| INV-271 | ok | ok — both signatures exist once S4 lands | testscript: dump and load respond to their §6.2 signatures; anything extra refuses |
| INV-318 | ok | ok | header-version-selects-parser-before-meta: a dump whose header says an unknown version refuses BEFORE any data parsing (v1 is the only compiled-in version; the fixture proves order by showing the meta row is never reached) |
| INV-319 | ok | ok | header-meta-version-mismatch-refused |
| INV-365 | ok — review-class: the property is architectural | ok | code-review at close + the grammar red fixtures (every non-whitelisted statement refuses) |
| INV-366 | ok | ok | tampered-ddl-one-byte-refused |
| INV-367 | ok | ok | non-whitelisted-statements-refused: PRAGMA, ATTACH, an expression in VALUES, an unknown table, a wrong column list — each exit 2 |
| INV-368 | ok | ok | load-connection-hardening-active: trusted_schema=0, cell_size_check=1, mmap_size=0, capped limits, asserted on the load connection |
| INV-369 | ok | ok | missing-required-meta-row-refused |
| INV-370 | ok | ok | same fixture as INV-319 (the two rows state the §8.1 and §8.5 sides of one refusal) |
| INV-371 | ok | ok | loaded-db-carries-stamped-version |
| INV-372 | ok | ok | events-highwater-seeded-max-boundary-liveseq: a dump with an events gap above the boundary; next insert does not reuse |
| INV-373 | ok — see the architectural note above | ok | forged-boundary-aborts-before-rename + trailless-terminal-aborts-before-rename; temp file gone after abort |
| INV-374 | ok | ok | load-lands-by-rename: the target path never holds a partial database (probe: no db file exists mid-refusal) |
| INV-375 | ok | ok | doc-review at close: grep the driver source for DBCONFIG_DEFENSIVE (absent), note recorded in code |
| INV-376 | ok | ok | FuzzWhitelistParser exists with a seed corpus; `make fuzz` runs it |
| INV-498 | ok | ok with note — the fuzz TARGET and corpus are local deliverables; the CI job is a first-push gate, so the row stays `planned` at close (S0 precedent) | fuzz target + CI at first push |
| INV-503 | ok | ok — a §16 re-verification probe, S1c's pattern | vacuum-into-rename-probe: VACUUM INTO a temp file on the pinned driver, rename, reopen, integrity ok |

## What this open did not do

No code was written. The close review should check two seams hard: the
rules subset S4 builds for INV-373 must not quietly become S7's verify
(only R-checks that load's abort path needs, no verb, no reporting
contract — S7 owns those); and `load --force` semantics at S4 are the
minimal §8.3 sentence (an existing database is discarded only under
--force, after a divergence summary) — the full §8.4 sidecar matrix stays
S8b's, and any comparison logic beyond the summary line is scope creep.
