# S12 opening record

Stage: S12 — pilot ladder rungs 3–4 and the remaining §16 deliverables
(frozen plan §4 "S12"; spec §16 D13). Story: v0-bootstrap/S19, started
2026-07-24. Spec revision at open: 3.23.

## Scope restated from the spec and plan (re-read at open)

1. **Rung 3 — disposable-clone import rehearsal.** A disposable local
   clone of the pilot client (a private local project; never named in
   this repository's tracked files) kept under a gitignored path —
   refreshed by pull, imported from scratch each round, real git
   objects, zero risk to the source (§16 D13). Verification per the
   plan: repeated from-scratch imports of the corpus derived from the
   client's backlog, `verify` green each round, importer defects filed
   as tracker records. The corpus and every rehearsal artifact carrying
   client content stay under the gitignored path.
2. **Rung 4 — colocated live install**, only after the importer
   round-trips the full corpus with `verify` green: the host's existing
   gates stay authoritative, selftracked chains from them (§9),
   abandonment = delete a directory. The plan's checklist:
   host-gates-stay-authoritative executed.
3. **Documents:** README final (positioning + public references to the
   `docs/research/` documents), CONTRIBUTING (DCO + AI-contribution
   clause), the generic migration guide — walked against the S9 fixture
   corpus (this is also where INV-449/450 land, amendment
   `import-guide-reviews-ride-to-s12`) — and a docs link-check.
   Licensing: NOTICE presence re-checked against §16's deliverable line.

## Preconditions checked at open

- S18 (stage S11) closed 2026-07-24: the version gate stands before
  every verb, spec at rev 3.23, `make gates` exit 0, full `verify` 0
  violations — the importer the rehearsal drives is the reviewed S9
  door behind the live gate.
- `/work/local/` is gitignored (the repo's own ignore file), and the
  path dictionary's `local` root exists: the disposable clone, the
  derived corpus, and rehearsal notes live there and only there.
- The pilot client exists locally and is reachable read-only for
  cloning; the rehearsal touches only the clone.

## Watch-items taken into the stage

- **Privacy is the sharpest edge of this stage.** Nothing derived from
  the client — names, titles, file paths, corpus text — may enter a
  tracked file, a commit message, or the tracker's own records beyond
  neutral wording ("the pilot client"). The close review runs a
  dedicated privacy/publication lens over the full tracked diff.
- **Rung 4 writes into a live repository that is not this one.** The
  install is spec-sanctioned (D13, owner-ratified) and reversible by
  design, but it proceeds only against a clean client tree, with the
  host's gates verified authoritative afterwards; anything murkier
  stops the ladder and surfaces to the owner.
- **Importer defects found by the rehearsal are records, not patches**:
  each files as a tracker task (or story, if worth an epic) before any
  fix lands, per the rehearsal's stated purpose — rung 3 exists to find
  them.

## Close appendix (2026-07-24)

**Rung 3.** The corpus derived from the pilot client's backlog imported
on its second attempt (the first refusal became tracker task #8, the
silent-citation observation task #9); from-scratch rounds ran full
`verify` to 0 violations with per-round artifacts archived under the
gitignored path, and cross-round determinism was proven by
timestamp-normalized byte-identity — synthesized wall-clock timestamps
are the only varying bytes, exactly the designed `--legacy` behavior.

**Rung 4.** The live colocated install: incumbent hooks detected and
chained per the recipe (subprocess calls, exit propagation, post-commit
at top), the host's gate authoritative, full `verify` 0 violations, and
a minimal footprint — three modified files plus new generated ones, all
left uncommitted for the owner's review. `.claude/settings.json`
untouched, verified by diff.

**The review (plan §5, FULL).** Three fresh critics — privacy/
publication, document fidelity, rehearsal mechanics. Privacy swept the
full tracked diff, the tracker dump (zero Cyrillic, zero client
strings), commit messages, and a ten-string distinctive-substring
git-grep: clean except aggregate corpus counts in one commit message
(MINOR — neutralized at the first-push squash, rider recorded on task
#5). Document fidelity: the abandonment claim was false for the chained
case — reproduced as a host hook failing exit 127 after directory
deletion — and the README/guide now state the one-line removal (task
#10 files the guarded-line alternative); the `--legacy` terminal list
gained `DUPLICATE`; "unignorable" became the recorded-one-shot-bypass
truth; `future_increment`, `legacy_reason` and `dup_of`'s assigned-id
semantics entered the guide; INV-449/450/453 were confirmed satisfied.
Rehearsal mechanics re-ran everything read-only and found the sharpest
lesson of the stage: the derivation script had silently corrupted
pipe-containing rows, misattributed commit citations across card
boundaries, and dropped two cards — so the first "verify green" was
evidence about a reduced corpus, not the source. Derivation fixed,
rounds re-run, live install redone with the corrected corpus before
close. Adjudicated-and-refuted findings are on the record in the review
transcripts; the frozen plan's "defects filed as stories" wording is
satisfied by tracker tasks per the opening record's restatement.

**Verification per the plan's S12 row:** repeated from-scratch imports
with `verify` green each round ✓ (archived); importer defects filed as
tracker records ✓ (#8, #9); colocated install with the
host-gates-stay-authoritative checklist ✓ (chained per recipe, host
hook's own paths preserved, combined run exit 0); migration guide
walked against the fixture corpus ✓ (the guide's example round-trips
through import+verify — re-verified independently by the fidelity
critic); docs link-check ✓ (scripts/check-doc-links.sh, all relative
links resolve). Public record:
docs/research/2026-07-24-pilot-import-rehearsal.md. The tracker's own
record of this close is worklog v0-bootstrap/S19.
