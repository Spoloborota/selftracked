# S6 opening record

Stage: S6 — epic/story/worklog/criteria verbs (plan §4). Opened
2026-07-20, per D-EP13. Spec revision at open: 3.15. Plan revision at
open: 13. Rows owned at open: 80. After the open: 73 — seven moved.

Scope: the epic lifecycle (create/activate/pause/dissolve/show/list/
close — close with its six refusal conditions including the ratified
cardinality condition), the story state machine (add/ready/start/block/
unblock/done/dissolve) with WIP/DoR, `worklog add` restricted to V-rows
and corrections, `criteria` (add/met/check) with runnable execution — and
the S5a-deferred half of `edit`: the epic/story field flags
(--goal/--dod/--consumes/--produces), whose obligation rides the already
closed INV-199 and re-verifies at this close.

**Resolved verification commands.** Scenario files per verb family:
epic.txtar, story.txtar, worklog.txtar, criteria.txtar, plus dumpdiff
shapes for INV-332/333 and the edit-extension re-verification inside
epic.txtar.

## Rows moved (placement defects)

| Rows | To | Why |
|---|---|---|
| INV-260, INV-269, INV-270 | S9 | Importer obligations (date priority, per-epic append order, story materialization) — `import` is S9's verb. |
| INV-481, INV-482, INV-483 | S12 | The §12 T0–T10 trace as designated e2e material needs init, prime, hooks and import — assembled only at the pilot-ladder stage. |
| INV-117 | S7 | R10's idle-clock exclusion of correction rows is a verify rule; R10 is built at S7. |

## Notable realizations (the rest are their row texts)

- INV-019/060/286: `epic close` is ONE transaction — criteria check,
  status sweep, close_sweep stamp, events row, with `active_verb` written
  and deleted inside it (crash ⇒ rollback removes it). The S1b close-gate
  trigger demands exactly this choreography.
- INV-016/281–285: the six refusal conditions, each its own red fixture,
  plus the complete-blocker-list property (one failed close names ALL
  blockers, §6.4 "refuses with the complete blocker list").
- INV-226–229: `criteria check` runs `$ `-commands from repo root with a
  per-command timeout, records evidence, flips met 1→0 on regression,
  exits 1 on failure.
- INV-235: `story start` = READY + empty blocked (DoR) + WIP slot free —
  three distinct refusals.
- INV-237: status flip + DONE worklog row in one transaction (the
  pipeline's mutate closure IS that transaction).
- INV-240–246: the `PO:` prefix is authoring convention — verbs enforce
  presence of resolution text only; fixtures assert the text lands
  verbatim in the events row.
- INV-296: `worklog add` validates story membership / V-form at the verb;
  R4 re-checks at S7.
- INV-332/333: dumpdiff shapes (3 lines for done, 2 for unblock).

## What this open did not do

No code was written. Close review seams: `criteria check` executes
repo-controlled shell commands — the §6.2 threat-model sentence (same
trust decision as running the repo's build) must ride in the code as a
comment, not silently; and `epic dissolve`'s auto-DISSOLVE of
PLANNED/READY/BLOCKED stories must write per-story worklog rows in the
same transaction.
