# S10 opening record

Stage: S10 — the dogfood switchover (plan §4; spec §16 D13). Opened
2026-07-23, per D-EP13. Spec revision at open: 3.17. Plan revision at open:
17. Rows owned at open: 1 (INV-514) — most of the stage is plan-native
(§3 rule 5, D-EP5), so the single row understates the scope by design; the
plan-native items below are closed by the stage's review pass, not by rows.

## Scope: the tracker starts tracking its own project

Every stage so far exercised the tracker against synthetic fixtures. S10
turns it on itself: this repository's bootstrap records — the traceability
inventory and the progress ledger — are imported into `.selftracked/` as the
first epic, through `import`, the door S9 built and reviewed. The fossil
precedent the spec cites (§16): the first project hosted is the tool itself.

The plan-native work list (plan §4 S10 + §9, restated for the close review
to check against):

1. **Corpus authoring.** The import corpus is derived from
   `docs/v0-traceability-inventory.md` and `docs/v0-progress.md`. Format:
   the S9 reader's `md-table` and/or `json`. One authoring constraint is
   already on record (S9 escalation E3): md-table cannot express a bundled
   increment, so any ledger row bundling several increments must be split
   row-per-increment at authoring time — or authored in JSON, the format
   whose split path INV-444 actually exercises.
2. **`init` + `import` on this repo.** `.selftracked/` does not exist at
   open (verified: no `.selftracked/` in the worktree). `init` scaffolds it;
   `import` (with `--legacy` where history needs its three relaxations)
   lands the bootstrap epic. This is the importer's **first real rehearsal**
   — rung 2 of D13's pilot ladder — and importer defects found here are
   defects to fix, not to route around.
3. **Retirement.** The progress ledger is deleted; the inventory file
   retires (both stay readable in git history — plan §9; the inventory's
   rows live on as imported records). The amendments log moves into the
   tracker as records of the governing epic. The plan document itself
   freezes at S10 close as the bootstrap's historical record (§9).
4. **DoD.** `selftracked verify` exits 0 on this repo's live tracker.

## The row

| Row | Content | Placement | Verification resolved |
|---|---|---|---|
| INV-514 | **ok** — the row restates spec §16's D13 sentence faithfully (re-read against §16 at open, spec rev 3.17): backlog moves into `.selftracked/` via `import` from the implementation plan derived from this spec, tracked as the first epic. | **ok** — plan §4 owns the switchover at S10, and §16's own sentence names both `init` and `import` as ingredients: `init` landed at S8a, `import` at S9, so S10 is the first stage where the switchover is executable. The row's "immediately after init first works" clause reads as ordering (the switchover follows init working), not as a deadline S8a missed — the plan's placement note says the same. | The placeholder "test: confirm the first epic created via import corresponds to this spec's own implementation plan" resolves to commands at the repo root, recorded in the ledger's S10 row at close: `selftracked verify` exits 0 on the live tracker, and `selftracked show epic:<bootstrap-slug>` (plus the epic's presence in `state`/`prime` output) shows the first epic is the bootstrap epic — its goal citing this spec's implementation plan, its records derived from the inventory and ledger. |

No placement moves, no amendment needed at open: one row, executable here,
content current.

## Preconditions checked at open

- **The load-poison hazard is closed.** The ledger's S10 recommendation —
  fix the `set-status DUPLICATE` reverse-chain poison pill before
  self-hosting — was executed in the owner-ordered pre-S10 bugfix batch
  (commits `b3cadcf` + `9569d55`, one-critic round, `make gates` green on a
  cleared cache). A self-hosted tracker can no longer be made unloadable by
  two legal verbs.
- **Known-open items that do not block** (all on the ledger): the `pause`
  IN-PROGRESS-story orphan (open question 2 — an S6-surface decision, not
  an importer path); S9 escalations E1/E2 (explicit-date `--legacy`
  semantics and the calendar-day-disagreement recording — both about legacy
  date derivation; E1's shipped interim admits an explicit date without
  `--legacy`, which the corpus can rely on); the two spec-wording notes
  (§11.1 `load` prose, §9 rc-triage).

## Watch-items carried into the stage

- **E3 (from the S9 close).** The md-table reader cannot express a bundled
  increment; the ledger corpus is the first real test of whether that
  limitation bites. If authoring in md-table forces a lossy flattening
  anywhere, the answer is the JSON format or a row split — never widening
  the md-table grammar mid-stage (that would be S9 scope reopened, an
  amendment).
- **Corpus dates.** The ledger records stage closes as dated prose and the
  inventory carries evidence commits; the date engine is git-first
  (INV-260), so cited commits will out-vote narrative dates — which is the
  designed behavior, and any calendar-day disagreement warning during the
  import is corpus-audit material, not noise to suppress.
- **R12/terminal entities.** The bootstrap epic imports with terminal
  records (closed stages); `--legacy` is the sanctioned path for terminal
  INSERTs and every imported terminal entity gets its `import` events row
  (INV-446) — the corpus must not try to route around that.
