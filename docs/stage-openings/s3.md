# S3 opening record

Stage: S3 — serializer + `dump` (plan §4). Opened 2026-07-19, per D-EP13.
Spec revision at open: 3.13. Plan revision at open: 12.
Rows owned at open: 36. After the open: 22 at S3 — fourteen moved (below).

S3's machinery: the schema layer, the dispatcher, and what this stage
itself builds — the canonical serializer (§8.1) and the `dump` verb, the
catalog's first real verb. It builds no loader (S4), no write verbs (S5a),
no story verbs (S6), no `verify` (S7), no `init` (S8a), and CI has not run
(D-EP8). Every moved row needs one of those.

**Resolved verification command.** Serializer fixtures:
`go test ./internal/dump -run 'TestSerializer/<fixture>'`; verb-level and
diff-shape fixtures run as testscript scenarios
(`go test ./internal/cli -run TestScripts/<scenario>`).

## Rows moved (placement defects)

| Row | To | Why |
|---|---|---|
| INV-271 | S4 | The row covers both stub signatures; `load [--force]` exists only at S4, and a row cannot half-close. `dump [--stdout]`'s signature is asserted by S3's own scripts either way. |
| INV-318 | S4 | "Header `schema_version` selects DDL(k)/whitelist(k) before parsing" is loader behaviour. |
| INV-319 | S4 | Header↔meta version mismatch is a `load` refusal. |
| INV-141 | S5a | "The full OLD value lives ONLY in the prior dump" needs `edit` writing its bounded-prefix events row — S5a's verb — to prove the "only". |
| INV-142 | S5a | A double overwrite within one session is two `edit` calls between commits. |
| INV-327 | S5a | "Control characters refused at write time" is the write-verb pipeline's validation; the serializer never sees the value that was refused. |
| INV-330 | S5a | "A status flip diffs as two lines" binds what `set-status` writes; no verb, no flip. |
| INV-331 | S5a | "Creating verbs bump the seq header" binds `create`. |
| INV-332 | S6 | `story done`'s three-line diff needs the story verbs. |
| INV-333 | S6 | `story unblock`'s two-line shape, same. |
| INV-321 | S7 | R9's live-DB sequence sanity check is a verify rule. |
| INV-341 | S7 | The boundary-tamper red fixture asserts "R9 fails while R1 stays green" — both are verify rules. |
| INV-418 | S8a | "Fresh `init` seeds the meta rows" names the verb S8a builds; the underlying seeding is already proven at S1a (`TestSeededMetaRowsArePresent`). |
| INV-501 | S11 | The golden old-dump migration corpus exists "from schema version 2 on"; S11 owns versioned rebuild. Parking it on S3 would close the stage with a row nothing can execute before v2. |

## Per-row verdicts

| Row | Content | Placement | Verification (resolved) |
|---|---|---|---|
| INV-061 | ok | ok | dump-never-contains-active_verb-key: leak the key via raw SQL, serialize, assert absence |
| INV-140 | ok — the S3-provable half is "the full NEW value is in the entity row and therefore in every dump"; the events-prefix half is owned by S5a's edit rows | ok | full-new-value-recoverable-from-entity-row-and-dump |
| INV-314 | ok | ok — a documentation-fidelity check | doc-review at close: research doc §9 states the three disqualifiers verbatim (confirmed at this open: no column lists, sqlite_sequence rows, shadow-table blobs, 3.50.0 format change) |
| INV-315 | ok | ok | dump-utf8-lf-trailing-newline |
| INV-316 | ok | ok | dump-single-header-comment-only |
| INV-317 | ok | ok | header-carries-version-and-id-highwater-never-seq: bump events seq only; header byte-identical |
| INV-320 | ok — the serializer half; the load-resume half rides S4's roundtrip rows | ok | dump-has-no-sqlite_sequence-inserts |
| INV-322 | ok | ok | dump-ddl-block-byte-equals-compiled-ddl |
| INV-323 | ok | ok | dump-table-order-fixed-events-last (insertion-order shuffled) |
| INV-324 | ok | ok | dump-rows-ordered-by-full-pk-events-bounded |
| INV-325 | ok | ok | dump-every-insert-names-columns |
| INV-326 | ok | ok | dump-apostrophe-doubles-no-backslash |
| INV-328 | ok | ok | dump-null-only-in-the-three-columns |
| INV-329 | ok | ok | dump-contains-no-pragma |
| INV-334 | ok | ok | long-note-one-line-untruncated |
| INV-335 | ok | ok — documentation-fidelity | doc-review at close: forks doc D7 entry carries the merge/seq-corruption evidence |
| INV-337 | ok | ok | boundary-above-zero-omits-archived-events (test harness sets the meta row raw) |
| INV-494 | ok | ok with note — the determinism TEST lands here and runs on this machine; the cross-OS matrix is a first-push CI gate, so the row stays `planned` at close exactly like S0's INV-492/499/513 | determinism fixture + CI matrix at first push |
| INV-497 | ok | ok with note — mutation probes run manually at close (the S1b pattern); the CI wiring rides the same first-push boundary | manual mutation round at close + CI later |
| INV-500 | ok — the suite exists since S2; the properties accrue: golden `cmp` on dumps starts here, in-script git fixtures arrive with S8b's hook tests | ok | testscript scenario with golden dump cmp |
| INV-507 | ok | ok with note — same first-push boundary as INV-494; locally the fixture proves run-to-run determinism, not OS-to-OS | determinism fixture; cross-OS at first push |
| INV-547 | ok | ok | absent-boundary-row-treated-as-zero |

## What this open did not do

No code was written. Two design notes the close review should check
against what actually lands: the `dump` verb writes via temp + atomic
rename (§8.3) and updates the SHA-256 sidecar (§8.4 lists `dump` among the
sidecar's writers) — but the divergence MATRIX is S8b's scope, so S3's
sidecar involvement is write-only, no comparison; and the serializer must
read `events_archived_through` through the absent-row-is-zero rule even
though no v0 path deletes the row.
