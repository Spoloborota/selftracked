# S2 opening record

Stage: S2 — CLI dispatcher (plan §4). Opened 2026-07-19, per D-EP13.
Spec revision at open: 3.13. Plan revision at open: 12.
Rows owned at open: 25. After the open: 20 at S2 — five moved (below).

S2's scope text: verb registry, §3.2 parsing obligations (a)–(c) plus the
leading-dash shape rule, JSON errors, §6.1 exit contract, version-gate
**stub**. Its DoD: a testscript usage-error matrix — exact exit codes,
JSON-only output, `-h` behaviour. S2 builds the binary and the dispatcher;
it builds no real verbs, no serializer, no hooks — rows needing those
moved.

**Resolved verification commands.** The e2e half runs through a testscript
suite (`go test ./internal/cli -run TestScripts/<fixture>`, txtar scripts
driving the real binary); unit-level fixtures run as named Go tests in the
package that owns them. Each row's line names its command.

## Rows moved (placement defects)

| Row | To | Why |
|---|---|---|
| INV-048 | S6 | Kebab-case slug validation and the no-rename property live in `epic create`/`edit` — S6's verbs. S2 has no epic verb to refuse anything. |
| INV-049 | S6 | The `S<number>` format's schema half is already proven (S1b, INV-100); the verb-side half — `story add` shaping ids, the reserved `V-<n>` namespace refusal — is S6's `story`/`worklog` verbs. |
| INV-050 | S5b | Scope resolution (default `''` vs named) is exercised by `paths set CLASS[@SCOPE]` and artifact linking — S5b's verbs. |
| INV-180 | S5a | The write-verb pipeline order is executable only where a real write verb exists; plan §4 places "write-verb pipeline" in S5a's scope text verbatim. S2 ships only the version-gate stub the pipeline will call. |
| INV-181 | S5a | Same: "read verbs run the gate first, then query_only, no side effects" needs a real read verb; S5a builds `show`/`list`/`ready`. |

## Per-row verdicts

| Row | Content | Placement | Verification (resolved) |
|---|---|---|---|
| INV-002 | ok | ok — the dispatcher half: a closed registry (unknown verb = usage refusal) and a structural `--json` guarantee (registration wires the flag; a verb cannot register without it). The rule-file surface of "no raw SQL" is S8b's rows | testscript: unknown-verb-refused + registry unit test `TestEveryRegisteredVerbHasJSON` |
| INV-020 | ok | ok — the reference grammar parser is dispatcher-adjacent and verbs consume it | `go test ./internal/ref -run TestGrammar` (shared with INV-045) |
| INV-025 | ok | ok, narrowed honestly — at S2 the audit can bind the layers that exist: an import-direction test (cmd → cli → schema, never reverse). The integration layer's thinness is re-audited by S8's own rows | `go test ./internal/cli -run TestDependencyDirection` |
| INV-034 | ok | ok with note — the gate itself has existed since S0 (`make fix-check`, `scripts/probe-gofix.sh` red-input probe); the row was left here by G0 and is closed by running that gate at this stage's close | `make fix-check` |
| INV-035 | ok | ok | audit: `grep -E 'cobra|urfave|kong' go.mod` exits 1 (no match) |
| INV-036 | ok | ok | testscript: arity-both-directions + leftover-args-refused + double-dash-honored |
| INV-037 | ok | ok | testscript: flag-parse-error-is-json-exit-2 |
| INV-038 | ok | ok | testscript: help-is-stdout-usage-exit-0 |
| INV-039 | ok | ok | testscript: leading-dash-positional-refused |
| INV-041 | ok — verification was `n/a`, now a real command | ok | audit: `grep sqlc go.mod` exits 1; hand-written repositories confirmed by import audit |
| INV-045 | ok | ok | `go test ./internal/ref -run TestGrammar`: all five forms parse, bare NN accepted, malformed refused |
| INV-046 | ok | ok — the `#NN` JSON rendering rule lives in the ref formatter every verb will use | `go test ./internal/ref -run TestTaskRefRendering` + testscript bare-integer acceptance |
| INV-047 | ok with note — the fixture as extracted ("delete max-id task") is unexecutable by design: deletes are trigger-refused (S1b). The never-reuse mechanism is AUTOINCREMENT, proven at S1b | `go test ./internal/schema -run 'TestGatesAutoincrement/tasks-id-never-reused'` |
| INV-176 | ok | ok | fixture: cli-binary-alias-strk — the build produces both names from one main; both `-h` exit 0 |
| INV-177 | ok | ok — structurally at the registry (every registered verb carries `--json`); the full catalog re-proves per verb as stages add them | registry unit test `TestEveryRegisteredVerbHasJSON` |
| INV-178 | ok | ok | testscript: json-error-shape — `{"error":{"code","message"}}` and nothing else on stderr/stdout |
| INV-179 | ok | ok — the mapper lands here (S1c proved the extended codes it leans on); full behavioural coverage grows with the verbs | `go test ./internal/cli -run TestExitMapper` + testscript usage-exit-2 |
| INV-182 | ok | ok | testscript: subverb-pop-order (stub two-level verb) |
| INV-183 | ok | ok | testscript: subverb-arity-check |
| INV-184 | ok | ok — the elision rule is a property of the catalog the registry declares; at S2 it binds the registry's declaration mechanism, and each verb stage's close re-checks its own entries against §6.2 | registry unit test `TestSiblingSubverbElision` |

## What this open did not do

No code was written. The testscript dependency (the §16 e2e harness) does
not exist in go.mod yet — adding it is S2 implementation work and must
follow the §3.2 pin policy (exact version, recorded, `check-pins` green).
The close review should re-check the narrowed realizations (INV-002,
INV-025, INV-177, INV-179, INV-184) against their row texts: each claims a
structural-at-S2 / re-proven-later split, and that split is this record's
judgment, not the spec's text.
